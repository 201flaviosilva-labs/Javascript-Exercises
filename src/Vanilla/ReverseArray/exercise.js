/**
 * @description
 * Reverse the given array without using the array.reverse() function {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reverse}
 * 
 * @example reverseArray([0]); // [0]
 * @example reverseArray([0,1,2,3]); // [3,2,1,0]
 * @example reverseArray([0,1,2,3,4]); // [4,3,2,1,0]
 * @example reverseArray(["one","two","three"]); // ['three','two','one']
 * @example reverseArray([true,false,false]); // [false,false,true]
 * @example reverseArray([{ one: 1 },{ two: 2 },{ three: 3 }]); // [{three:3},{two:2},{one:1}]
 * 
 * 
 * @param {any[]} arr - the array to reverse
 * @returns {any[]}
 */
export function reverseArray(arr) {
	return;
}
