/**
 * @description
 * Return average of the given numbers
 * 
 * @example average([0, 5, 10]); // 5
 * @example average([10, 5, 7.5, 15, 0]); // 7.5
 * 
 * @param {number[]} numbers - a array of numbers
 * @returns {number}
 */
export function average(numbers) {
	return;
}
