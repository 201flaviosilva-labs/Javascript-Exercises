/**
 * @description
 * Based on the 3 numbers given, check if it is possible to create a triangle and what kind of triangle
 * 
 * @example checkTriangleType(0, 1, 2); // Impossible
 * @example checkTriangleType(10, 10, 10); // Equilateral
 * @example checkTriangleType(10, 10, 5); // Isosceles
 * @example checkTriangleType(9, 10, 5); // Scalene
 * 
 * @param {number} segment1 - the first segment
 * @param {number} segment2 - the second segment
 * @param {number} segment3 - the third segment
 * @returns {string}
 */
export function checkTriangleType(segment1, segment2, segment3) {
	return;
}
